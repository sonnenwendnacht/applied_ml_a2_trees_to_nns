# Historical coursework figures

These five PNG files were copied unchanged from the PC's original `aml/project2`
folder. They accompany the original `assignment2.ipynb` coursework and are
preserved as historical artifacts, not outputs of the new `compare.py` script.

- `gbdt_feature_importance.png`
- `gbdt_learning_rate_comparison.png`
- `mlp_architecture_comparison.png`
- `mlp_training_loss.png`
- `model_comparison_pr_curve.png`

The historical experiment includes post-call `duration` and preprocessing fitted
before inner cross-validation. See [validation notes](../VALIDATION.md) for the
methodology differences and why its scores should not be compared directly
with the new sample run. The complete original notebook sweeps were not rerun.
